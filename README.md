# كافيه دافئ - موقع ويب احترافي

موقع ويب كامل للكافيه بتصميم احترافي ودافئ بالدرجات البنية، مطور باستخدام Vue 3 و Vite.

## المميزات

- 🎨 تصميم احترافي بألوان دافئة (درجات البني والكريمي)
- 📱 متجاوب مع جميع الأجهزة (موبايل/تابلت/ديسكتوب)
- ☕ صفحة منيو شاملة مع فلترة المنتجات
- 👤 نظام تسجيل دخول وإنشاء حساب مع التحقق من كلمة المرور
- 🛒 نظام عربة تسوق
- 📄 صفحات تفصيلية للمنتجات
- ℹ️ صفحة "عنّا" شاملة
- 🔍 بحث متقدم في المنتجات

## متطلبات النظام

- Node.js (الإصدار 16 أو أحدث)
- npm أو yarn

## التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. تشغيل Mock API Server

```bash
npm run json-server
```

هذا سيشغل خادم البيانات على `http://localhost:3001`

### 3. تشغيل الموقع

```bash
npm run dev
```

هذا سيشغل الموقع على `http://localhost:3000`

## هيكل المشروع

```
cafe-website/
├── public/
├── src/
│   ├── components/
│   │   ├── Navbar.vue
│   │   └── Footer.vue
│   ├── pages/
│   │   ├── Home.vue
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   ├── Menu.vue
│   │   ├── About.vue
│   │   └── ProductDetail.vue
│   ├── services/
│   │   └── api.js
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── db.json
├── package.json
├── vite.config.js
└── README.md
```

## الصفحات المتاحة

- **الرئيسية** (`/`) - الصفحة الرئيسية مع المنتجات المميزة
- **تسجيل الدخول** (`/login`) - تسجيل دخول المستخدمين
- **إنشاء حساب** (`/register`) - تسجيل مستخدمين جدد
- **المنيو** (`/menu`) - عرض جميع المنتجات مع الفلترة
- **عنّا** (`/about`) - معلومات عن الكافيه والفريق
- **تفاصيل المنتج** (`/product/:id`) - صفحة تفصيلية لكل منتج

## مميزات نظام التسجيل

- التحقق من كلمة المرور:
  - 12 حرف على الأقل
  - حرف كبير واحد على الأقل
  - حرف خاص واحد على الأقل (!@#$%^&*())
- حفظ البيانات في localStorage
- واجهة API وهمية للاختبار

## نظام الألوان

- **البني الداكن**: `#8B4513`
- **البني المتوسط**: `#A0522D`
- **البني الفاتح**: `#D2B48C`
- **البني الفاتح جداً**: `#F5E6D3`
- **الكريمي**: `#FFF8DC`
- **الذهبي**: `#DAA520`

## التقنيات المستخدمة

- **Vue 3** (Options API)
- **Vue Router** للتنقل
- **Vite** كأداة البناء
- **JSON Server** للـ Mock API
- **CSS Variables** لنظام الألوان
- **Flexbox & Grid** للتخطيط

## أوامر التشغيل

```bash
# تثبيت المتطلبات
npm install

# تشغيل خادم التطوير
npm run dev

# تشغيل Mock API
npm run json-server

# بناء المشروع للإنتاج
npm run build

# معاينة البناء
npm run preview
```

## ملاحظات مهمة

1. تأكد من تشغيل `json-server` قبل تشغيل الموقع
2. البيانات محفوظة في `localStorage` للاختبار
3. الصور من Unsplash للعرض التوضيحي
4. التصميم متجاوب مع جميع أحجام الشاشات

## الدعم

إذا واجهت أي مشاكل، تأكد من:
- تثبيت Node.js بشكل صحيح
- تشغيل الأوامر من مجلد المشروع الصحيح
- عدم وجود تطبيقات أخرى تستخدم نفس المنافذ (3000, 3001)

---

تم تطوير هذا المشروع باستخدام Vue 3 و Vite مع تصميم احترافي ومميزات متقدمة.

